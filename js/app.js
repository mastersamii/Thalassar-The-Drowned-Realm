"use strict";
document.addEventListener("DOMContentLoaded",()=>{initTheme();initMenu();initBackToTop();initSearch();initQuote();initYear();});
function initTheme(){const saved=localStorage.getItem("thalassar-theme")||"dark";document.documentElement.setAttribute("data-theme",saved);const btn=document.getElementById("themeBtn");if(!btn)return;btn.addEventListener("click",()=>{const cur=document.documentElement.getAttribute("data-theme");const next=cur==="dark"?"light":"dark";document.documentElement.setAttribute("data-theme",next);localStorage.setItem("thalassar-theme",next);});}
function initMenu(){const btn=document.getElementById("menuBtn");const links=document.getElementById("navLinks");if(!btn||!links)return;btn.addEventListener("click",()=>links.classList.toggle("open"));}
function initBackToTop(){const btn=document.getElementById("topBtn");if(!btn)return;window.addEventListener("scroll",()=>{btn.style.display=window.scrollY>500?"block":"none";});btn.addEventListener("click",()=>window.scrollTo({top:0,behavior:"smooth"}));}
function initSearch(){const input=document.getElementById("searchInput");if(!input)return;input.addEventListener("input",()=>{const q=input.value.toLowerCase().trim();document.querySelectorAll(".searchable").forEach(s=>{if(!q){s.classList.remove("hidden-by-search");return;}s.classList.toggle("hidden-by-search",!s.innerText.toLowerCase().includes(q));});});}
async function initQuote(){const box=document.getElementById("dailyQuote");if(!box)return;try{const r=await fetch("data/quotes.json");const quotes=await r.json();const q=quotes[Math.floor(Math.random()*quotes.length)];box.innerHTML=`<p>“${q.quote}”</p><p><strong>${q.author}</strong></p>`;}catch{box.innerHTML="<p>“The Sea Remembers Every Name.”</p><p><strong>Atlantean Proverb</strong></p>";}}
function initYear(){const y=document.getElementById("year");if(y)y.textContent=new Date().getFullYear();}
